package main;

public class NeighborsStringComp
{
    public static void main(String [] args) {
        
    }
    
    void algo(FastKmerSearchData d, FileInputs m) {
        
        /*
        def neighbors(pattern,d):
        if d == 0:
        return pattern
        if len(pattern) ==1:
        return 'ACGT'
        neighborhood = set()
        suffixNeighbors = neighbors(suffix(pattern),d)
        for tx in suffixNeighbors:
          if hamming_distance(tx,suffix(pattern)) < d
              and hamming_distance(tx,suffix(pattern)) !=0:
            for n in "ACGT":
              if n!=first_symbol(pattern):
                neighborhood.add(n+tx) 
        return neighborhood
        */
        
    }
}
